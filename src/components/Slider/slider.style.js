import styled from 'styled-components';

export const SliderMax = styled.input`
  /* display: none; */
`;

export const SliderMin = styled.input`
  position: absolute;
  background-color: transparent;
  -webkit-appearance: none;
  /* -webkit-appearance: none;
  -moz-appearanc  /* -webkit-appearance: none;
e: none;
  appearance: none; */
  /* width: 20%; */
  left: 0;
  z-index: -1;
`;

export const Wrapper = styled.div`
  position: relative;
`;
